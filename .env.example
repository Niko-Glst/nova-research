# Copy this file to .env and fill in REAL values there.
# NEVER put real secrets in this file (.env.example) — it is tracked by git.
# .env itself is gitignored and must never be committed.

# --- Reddit API (praw) ---
# https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=

# --- News API ---
NEWS_API_KEY=

# --- Broker integration (paper/sandbox endpoint only) ---
# If a broker integration is ever added, its base URL must point at a
# paper-trading/sandbox endpoint here, never a live-trading endpoint.
BROKER_API_KEY=
BROKER_API_SECRET=
BROKER_BASE_URL=

# --- General ---
LOG_LEVEL=INFO
