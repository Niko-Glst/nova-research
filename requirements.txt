# Core
python-dotenv>=1.0
pyyaml>=6.0
pandas>=2.0
numpy>=1.24

# Market data & technical analysis
yfinance>=0.2

# Reddit sentiment
praw>=7.7

# Backtesting: implemented directly in numpy (backtests/backtest_engine.py).
# vectorbt was the original choice, but it fails to import against plotly 7
# and pulls numba/llvmlite, which routinely fail to build on current Python.
# A long-only vectorized simulation is a few dozen lines of numpy, so the
# dependency bought little.

# NOTE: pandas-ta is intentionally NOT used. It is unmaintained and breaks on
# numpy 2.x; ta_analyst.py computes RSI/MACD/SMA directly in pandas instead.

# Dev / quality
pytest>=7.0
pre-commit>=3.0
detect-secrets>=1.4

# NOTE: Playwright is intentionally NOT listed here. Browser automation runs
# via the separate Playwright MCP server, not as a Python dependency of this repo.
